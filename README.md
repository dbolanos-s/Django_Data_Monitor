# Backend Analytics Server

Proyecto Django para construir un dashboard con renderizado del lado del servidor (SSR). En esta etapa la aplicación `dashboard` renderiza una plantilla HTML desde Django y utiliza archivos estáticos locales. El consumo de APIs externas con `requests` todavía no está implementado.
